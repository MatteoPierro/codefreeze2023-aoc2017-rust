#[cfg(test)]
mod spiral_memory {

    #[test]
    fn test_max_value() {
        
        assert_eq!(0,spiral_memory_distance(1));
    }

    fn spiral_memory_distance(square: i32) -> i32 {
        0
    }
}